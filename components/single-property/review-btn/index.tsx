import ReviewButton from "./review-button";

const ReviewStickBtn = ({ propertyId }: { propertyId: string }) => {
  return <ReviewButton propertyId={propertyId} />;
};

export default ReviewStickBtn;
